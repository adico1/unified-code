name: test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  functional:
    name: Functional suite (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test]"

      - name: Run functional tests
        run: pytest

      # Full gauntlet PASS is mandatory. A green unit suite alone is insufficient.
      - name: Run Unified Code gauntlet
        run: |
          set -euo pipefail
          uc gauntlet examples/declarations/text_stats_v2.json > /tmp/uc-gauntlet.json
          python - <<'PY'
          import json, sys
          report = json.load(open("/tmp/uc-gauntlet.json", encoding="utf-8"))
          if report.get("state") != "valid":
              print(report)
              raise SystemExit(f"gauntlet state={report.get('state')}")
          value = report.get("value") or {}
          if value.get("verdict") != "pass" or value.get("checks_failed", 1) != 0:
              print(value.get("failed_checks"))
              raise SystemExit(
                  f"gauntlet verdict={value.get('verdict')} "
                  f"failed={value.get('checks_failed')}"
              )
          for level in ("G0", "G1", "G2", "G3", "G4", "G5", "G6", "G7", "G8"):
              entry = (value.get("levels") or {}).get(level) or {}
              if entry.get("verdict") != "pass":
                  raise SystemExit(f"{level} failed: {entry.get('failed_checks')}")
          print(
              f"gauntlet PASS "
              f"{value.get('checks_passed')}/{value.get('checks_executed')}"
          )
          PY

      # CI verifies benchmark *logic* via deterministic unit tests above.
      # Cloud-runner wall-clock time is NOT the authority for L9.
      # Authoritative L9 measurement: local `uc benchmark --iterations 20`.

  l13:
    name: L13 Complete Testing Gauntlet
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test]"
          sudo apt-get update -qq && sudo apt-get install -y -qq gcc gcovr || true

      - name: Build C host
        run: make -C c posix CFLAGS="-std=c99 -Wall -O2 -Iinclude -Ithird_party -Icore -Ihost/mcu"

      - name: L13 gauntlet (fail on any dimension below 100%)
        run: |
          set -euo pipefail
          chmod +x scripts/run_l13.sh
          export UEM_C="$PWD/c/build/uem-c"
          ./scripts/run_l13.sh

      - name: Assert coverage.json all dimensions OK
        run: |
          python - <<'PY'
          import json, sys
          rep = json.load(open("coverage.json", encoding="utf-8"))
          dims = rep.get("dimensions") or {}
          bad = []
          for k, d in sorted(dims.items()):
              if not d.get("ok"):
                  bad.append(f"{k}: actual={d.get('actual')} required={d.get('required')}")
          if rep.get("verdict") != "pass" or bad:
              print("FAIL dimensions:", bad or rep)
              raise SystemExit(1)
          print("L13 PASS — all", len(dims), "dimensions at 100%")
          PY

  standard_ten:
    name: Standard Ten (governing contract)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[test]"

      - name: STANDARD_TEN.md present
        run: test -f STANDARD_TEN.md && test -f seed/ROOT.seed.json

      - name: standard.gap is only fallback refusal
        run: |
          python - <<'PY'
          from unified.standard import standard_gap, refuse_conventional
          g = standard_gap({"value": {"gap_id": "gap.ci", "rule": "3", "summary": "ci"}, "depths": (), "axes": (), "evidence": (), "state": "formed"})
          assert g["state"] == "invalid" and "standard.gap" in g["evidence"]
          r = refuse_conventional({"value": {"summary": "oop"}, "depths": (), "axes": (), "evidence": (), "state": "formed"})
          assert r["state"] == "invalid"
          print("standard.gap OK")
          PY

      - name: Generate UEM from seed declarations
        run: |
          python - <<'PY'
          from unified.standard_generate import generate_all_seed_declarations
          r = generate_all_seed_declarations()
          assert r.get("state") == "formed", r
          print(r["value"]["generated"])
          PY

      - name: Provenance audit
        run: python scripts/audit_standard_ten.py audit

      - name: Clean-room partial (UEM artifacts)
        run: |
          chmod +x scripts/clean_room_ten.sh
          ROOT="$PWD" bash scripts/clean_room_ten.sh

      - name: Reject conventional claim of TEN completion
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          m = json.loads(Path("PROVENANCE_MANIFEST.json").read_text(encoding="utf-8"))
          # While gaps open, verdict must not be silently pass
          open_gaps = m.get("open_gaps") or []
          illegal = m.get("illegal_provenance_count") or 0
          if open_gaps or illegal:
              assert m.get("verdict") == "fail", m.get("verdict")
              print("honest fail while migration open:", len(open_gaps), "gaps,", illegal, "illegal files")
          else:
              # Only when fully migrated
              assert m.get("verdict") == "pass"
              print("TEN complete")
          PY

      - name: Enforcement must not greenwash gaps
        run: |
          set +e
          python scripts/audit_standard_ten.py enforce
          code=$?
          set -e
          python - <<PY
          import json
          from pathlib import Path
          m = json.loads(Path("PROVENANCE_MANIFEST.json").read_text(encoding="utf-8"))
          open_gaps = m.get("open_gaps") or []
          illegal = int(m.get("illegal_provenance_count") or 0)
          code = $code
          if open_gaps or illegal:
              assert code != 0, (code, "enforce must fail while gaps/illegal provenance exist")
              print("enforce non-zero OK under open migration")
          else:
              assert code == 0, code
              print("enforce pass — TEN complete")
          PY
